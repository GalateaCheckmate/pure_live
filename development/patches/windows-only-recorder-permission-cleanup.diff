diff --git a/lib/recorder/pages/recorder/recorder_controller.dart b/lib/recorder/pages/recorder/recorder_controller.dart
--- a/lib/recorder/pages/recorder/recorder_controller.dart
+++ b/lib/recorder/pages/recorder/recorder_controller.dart
@@
 import 'package:open_filex/open_filex.dart';
 import 'package:pure_live/common/index.dart';
-import 'package:device_info_plus/device_info_plus.dart';
 import 'package:pure_live/common/utils/hive_pref_util.dart';
@@
-  Future<bool> requestStoragePermission() async {
-    if (!Platform.isAndroid) return true;
-
-    try {
-      final deviceInfo = DeviceInfoPlugin();
-      final androidInfo = await deviceInfo.androidInfo;
-      final sdkInt = androidInfo.version.sdkInt;
-      if (sdkInt >= 30) {
-        if (await Permission.manageExternalStorage.isGranted) {
-          return true;
-        }
-        final status = await Permission.manageExternalStorage.request();
-        if (status.isGranted) {
-          return true;
-        }
-      } else {
-        if (await Permission.storage.isGranted) {
-          return true;
-        }
-        final status = await Permission.storage.request();
-        if (status.isGranted) {
-          return true;
-        }
-      }
-    } catch (e) {
-      final status = await Permission.storage.request();
-      if (status.isGranted) return true;
-    }
-
-    ToastUtil.show(i18n('no_storage'));
-    return false;
-  }
-
   Future<void> addTask({required LiveRoom room}) async {
-    final granted = await requestStoragePermission();
-    if (!granted) {
-      ToastUtil.show(i18n('no_storage'));
-      return;
-    }
-
     if (tasks.any((e) => e.roomId == room.roomId && e.platform == room.platform)) {
       return;
     }
diff --git a/lib/common/index.dart b/lib/common/index.dart
--- a/lib/common/index.dart
+++ b/lib/common/index.dart
@@
-export 'package:permission_handler/permission_handler.dart';
 export 'package:flv_lzc/fijkplayer.dart';
diff --git a/pubspec.yaml b/pubspec.yaml
--- a/pubspec.yaml
+++ b/pubspec.yaml
@@
-  permission_handler: ^12.0.1
-  device_info_plus: ^13.1.0
   hive_ce: ^2.15.1
